import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Flame } from "lucide-react";

export default function StudyStreakCard({ streak }) {
  return (
    <div className="relative p-5 rounded-2xl overflow-hidden h-full bg-orange-50">
       <div className="flex justify-between items-start mb-4">
        <div className="p-2.5 rounded-lg bg-orange-50 border border-orange-200/80">
          <Flame className="w-6 h-6 text-orange-500" />
        </div>
      </div>
      <p className="text-sm text-text-secondary font-medium mb-1">Study Streak</p>
      <p className="text-3xl font-bold text-text-primary">{streak} day{streak !== 1 ? 's' : ''}</p>
      <p className="text-orange-600 text-sm font-semibold mt-2">
          {streak > 0 ? "Keep the fire going! 🔥" : "Start your streak today!"}
      </p>
    </div>
  );
}
